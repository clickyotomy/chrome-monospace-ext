{
	"name": "chrome-monospace-ext",
	"version": "0.1",
	"manifest_version": 2,
	"description": "Sets  `Monaco' as the default font for monospace text.",
	"content_scripts": [{
    		"css": ["style.css"],
		"matches": ["<all_urls>"]
	}]
}

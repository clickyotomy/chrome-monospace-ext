chrome-monospace-ext
--------------------

A silly Chrome extension to set `Monaco' as the default font for monospace text.
Subtext: I don't like `Menlo'.

NOTES
	[+] The packed extension (`.crx') is under the `packed' directory.


